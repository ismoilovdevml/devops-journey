import { Tab, Tabs } from 'nextra-theme-docs';
import Card, { Cards } from '@components/core/Card';
import developingGuides from './ci-cd/_meta.en-US.json';
import orchestratingGuides from './konteyner/_meta.en-US.json';
import delegatingGuides from './k8s/_meta.en-US.json';
import databaseGuides from './database/_meta.en-US.json';
import webserverGuides from './web-server/_meta.en-US.json';

# Qo'llanmalar

Hamjamiyat tomonidan yozilgan dokumentatsiya va qo'llanmalar. Mavzular CI/CD, Konteynerlash, Kubernetes(k8s), Monitoring, network, load-balancing va boshqalarni o'z ichiga oladi.

<Tabs items={['▶️ CI-CD', '▶️ Konteynerlash', '▶️ Kubernetes(k8s)', '▶️ Database', '▶️ Web Serverlar']}>
  <Tab style={{ paddingTop: 8 }}>
    <Cards
      num={3}
      children={Object.keys(developingGuides).map((key, i) => (
        <Card
          key={i}
          title={developingGuides[key].title}
          description={developingGuides[key].description}
          icon={developingGuides[key].icon}
          href={developingGuides[key].href}
        />
      ))}
    />
  </Tab>
  <Tab>
    <Cards
      num={3}
      children={Object.keys(orchestratingGuides).map((key, i) => (
        <Card
          key={i}
          title={orchestratingGuides[key].title}
          description={orchestratingGuides[key].description}
          icon={orchestratingGuides[key].icon}
          href={orchestratingGuides[key].href}
        />
      ))}
    />
  </Tab>
  <Tab>
    <Cards
      num={3}
      children={Object.keys(delegatingGuides).map((key, i) => (
        <Card
          key={i}
          title={delegatingGuides[key].title}
          description={delegatingGuides[key].description}
          icon={delegatingGuides[key].icon}
          href={delegatingGuides[key].href}
        />
      ))}
    />
  </Tab>
  <Tab>
    <Cards
      num={3}
      children={Object.keys(databaseGuides).map((key, i) => (
        <Card
          key={i}
          title={databaseGuides[key].title}
          description={databaseGuides[key].description}
          icon={databaseGuides[key].icon}
          href={databaseGuides[key].href}
        />
      ))}
    />
  </Tab>
  <Tab style={{ paddingTop: 8 }}>
    <Cards
      num={3}
      children={Object.keys(webserverGuides).map((key, i) => (
        <Card
          key={i}
          title={webserverGuides[key].title}
          description={webserverGuides[key].description}
          icon={webserverGuides[key].icon}
          href={webserverGuides[key].href}
        />
      ))}
    />
  </Tab>
</Tabs>