import { Tab, Tabs } from 'nextra-theme-docs';
import Card, { Cards } from '@components/core/Card';
import ci-cdGuides from './ci-cd/_meta.en-US.json';
import konteynerlashGuides from './konteyner/_meta.en-US.json';
import k8sGuides from './k8s/_meta.en-US.json';
import databaseGuides from './database/_meta.en-US.json';

# Qo'llanmalar

Hamjamiyat tomonidan yozilgan dokumentatsiya va qo'llanmalar. Mavzular CI/CD, Konteynerlash, Kubernetes(k8s), Monitoring, network, load-balancing va boshqalarni o'z ichiga oladi.

<Tabs items={['▶️ CI-CD', '▶️ Konteynerlash', '▶️ Kubernetes(k8s)', '▶️ Database']}>
  <Tab style={{ paddingTop: 8 }}>
    <Cards
      num={3}
      children={Object.keys(ci-cdGuides).map((key, i) => (
        <Card
          key={i}
          title={ci-cdGuides[key].title}
          description={ci-cdGuides[key].description}
          icon={ci-cdGuides[key].icon}
          href={ci-cdGuides[key].href}
        />
      ))}
    />
  </Tab>
  <Tab>
    <Cards
      num={3}
      children={Object.keys(konteynerlashGuides).map((key, i) => (
        <Card
          key={i}
          title={konteynerlashGuides[key].title}
          description={konteynerlashGuides[key].description}
          icon={konteynerlashGuides[key].icon}
          href={konteynerlashGuides[key].href}
        />
      ))}
    />
  </Tab>
  <Tab>
    <Cards
      num={3}
      children={Object.keys(k8sGuides).map((key, i) => (
        <Card
          key={i}
          title={k8sGuides[key].title}
          description={k8sGuides[key].description}
          icon={k8sGuides[key].icon}
          href={k8sGuides[key].href}
        />
      ))}
    />
  </Tab>
  <Tab>
    <Cards
      num={3}
      children={Object.keys(databaseGuides).map((key, i) => (
        <Card
          key={i}
          title={databaseGuides[key].title}
          description={databaseGuides[key].description}
          icon={databaseGuides[key].icon}
          href={databaseGuides[key].href}
        />
      ))}
    />
  </Tab>
</Tabs>
